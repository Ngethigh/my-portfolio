# คู่มือการใช้งาน Portfolio Website (Usage Guide)

ยินดีด้วยครับ! ตอนนี้คุณมีเว็บไซต์ Portfolio สุดเท่เป็นของตัวเองแล้ว นี่คือวิธีใช้งานและปรับแต่งครับ

## 1. วิธีดูเว็บไซต์ (How to View)
ง่ายมากๆ ครับ:
- เปิดเข้าไปในโฟลเดอร์ `portfolio-website`
- ดับเบิ้ลคลิกไฟล์ **`index.html`**
- เว็บไซต์จะเปิดขึ้นมาใน Browser (Chrome, Edge, Safari) ให้ดูทันที

## 2. วิธีแก้ไขข้อมูลส่วนตัว (How to Customize)
คุณสามารถแก้เนื้อหาให้เป็นของคุณเองได้โดยการเปิดไฟล์ `index.html` ด้วยโปรแกรม Text Editor (เช่น VS Code, Notepad++)

### แก้ไขชื่อและตำแหน่งงาน
ค้นหาบรรทัดที่เขียนว่า:
```html
<h1 class="animate-text delay-1">Kraisit Robchana</h1>
<p class="animate-text delay-2">Automation QA & Robot Framework Specialist</p>
```
เปลี่ยนข้อความเป็นชื่อของคุณได้เลย

### แก้ไขข้อมูล "เกี่ยวกับฉัน" (About Me)
ค้นหา section `<section id="about" class="about">` และแก้ไขข้อความในแท็ก `<p>`

### แก้ไขรูปภาพ
1. เตรียมรูปของคุณ (แนะนำไฟล์ .jpg หรือ .png)
2. นำไปใส่ในโฟลเดอร์ `assets/images`
3. ในไฟล์ `index.html` เปลี่ยนโค้ดส่วนแสดงรูป (หรือ icon placeholder) จาก:
   ```html
   <div class="img-placeholder"><i class="fas fa-user-astronaut"></i></div>
   ```
   ให้กลายเป็น:
   ```html
   <img src="assets/images/your-photo.jpg" alt="My Photo" style="width:100%; border-radius: 20px;">
   ```

### เพิ่ม/ลบ โปรเจค
ในส่วน `<section id="projects" class="projects">` คุณจะเห็นบล็อก `<div class="project-card">`
- **แก้ไข**: เปลี่ยนข้อความใน `<h3>` (ชื่อโปรเจค) และ `<p>` (รายละเอียด)
- **เพิ่ม**: ก็อปปี้ทั้งบล็อก `.project-card` แล้ววางเพิมต่อท้าย
- **ลบ**: ลบบล็อกที่คุณไม่ต้องการทิ้ง

### เพิ่มไฟล์คู่มือ (Handbook)
ในส่วนของการ์ด "Handbook" ผมได้เตรียมลิงก์ไว้ให้แล้วครับ สิ่งที่คุณต้องทำคือ:
1. เตรียมไฟล์ PDF หรือเอกสารของคุณ
2. เปลี่ยนชื่อไฟล์ให้ตรงกับที่ตั้งไว้ (หรือจะไปแก้ชื่อใน HTML ก็ได้):
   - `user-manual.pdf` (สำหรับคู่มือการใช้งาน)
   - `admin-guide.pdf` (สำหรับคู่มือผู้ดูแลระบบ)
   - `system-report.pdf` (สำหรับรายงานสรุปผล)
3. นำไฟล์ทั้ง 3 ไปใส่ในโฟลเดอร์ `assets/handbooks/` (สร้างโฟลเดอร์นี้ไว้ให้แล้วครับ)

## 3. การนำไปใช้งานจริง (Deployment) บน MAMP (สำหรับมือใหม่)

เพื่อให้ระบบ "ส่งข้อความ" ทำงานได้ คุณต้องรันเว็บไซต์ผ่าน Web Server (MAMP) ครับ ทำตามขั้นตอนง่ายๆ นี้ได้เลย:

### ขั้นตอนที่ 1: เปิดโปรแกรม MAMP
1. เปิดโปรแกรม **MAMP** ในเครื่องของคุณ (ปกติจะอยู่ใน Applications)
2. กดปุ่ม **Start Servers**
3. รอจนไฟสถานะเป็นสีเขียว (แปลว่า Server ทำงานแล้ว)

### ขั้นตอนที่ 2: ย้ายไฟล์เข้า Server
1. ย้ายโฟลเดอร์ `portfolio-website` นี้ ไปวางไว้ที่โฟลเดอร์ **htdocs** ของ MAMP
   - บน Mac ปกติจะอยู่ที่: `/Applications/MAMP/htdocs/`
2. (แนะนำ) เปลี่ยนชื่อโฟลเดอร์ให้สั้นๆ จำง่าย เช่น `my-portfolio`

### ขั้นตอนที่ 3: เปิดดูใน Browser
1. เปิด Google Chrome หรือ Safari
2. พิมพ์ในช่อง URL ว่า: `http://localhost:8888/my-portfolio` (หรือชื่อโฟลเดอร์ที่คุณตั้ง)
   - หมายเหตุ: MAMP มักจะใช้ Port 8888 เป็นค่าเริ่มต้น
3. กด Enter... ผ่าม! เว็บไซต์ของคุณออนไลน์ (ในเครื่อง) แล้วครับ 🎉

## 4. อยากให้คนอื่นเข้าดูเว็บเราได้? (Public Deployment)
ถ้าอยากส่งลิงก์ให้คนอื่นดู (ที่ไม่ใช่แค่ในเครื่องเรา) อ่านวิธีทำละเอียดได้ที่ไฟล์:
👉 **[DEPLOYMENT.md](DEPLOYMENT.md)**

### 📂 ไฟล์ข้อความ (Message Log)
**หมายเหตุ**: ตอนนี้ระบบส่งข้อความเป็นแบบ **จำลอง (Simulation)** เพื่อให้สามารถนำขึ้น GitHub Pages ได้ง่าย เวลากดส่งจะแสดง Alert สำเร็จแต่ไม่มีการบันทึกข้อมูลจริงครับ

---
**Tips:**
- ไฟล์ **`css/style.css`**: สำหรับแก้ไขสีและเลย์เอาต์
- ไฟล์ **`js/script.js`**: สำหรับแก้ไขลูกเล่นและการเคลื่อนไหว

ขอให้สนุกกับการปรับแต่งเว็บไซต์นะครับ!
