# วิธีทำให้เว็บไซต์ออนไลน์ (Deploy to GitHub Pages) 🎉

เพื่อให้เว็บไซต์ของคุณใช้งานได้จริง 100% (รวมถึงระบบส่งข้อความ) โดยที่คุณไม่ต้องเสียค่าใช้จ่าย ให้ทำตามขั้นตอนดังนี้ครับ:

## ส่วนที่ 1: ตั้งค่าระบบส่งข้อความ (Formspree)
1. เข้าไปที่ [Formspree.io](https://formspree.io/) แล้วสมัครสมาชิกฟรี
2. กดปุ่ม **"New Form"**
3. ตั้งชื่อฟอร์ม (เช่น Portfolio Contact) และใส่ Email ที่คุณต้องการให้ข้อความส่งไปหา
4. เมื่อสร้างเสร็จ คุณจะได้ **Form ID** (เช่น `https://formspree.io/f/mqakpogv` ตัวหลังสุดคือ ID)
5. เปิดไฟล์ `js/script.js` และนำไอดีไปใส่ในตัวแปร:
   ```javascript
   const FORMSPREE_ID = "ใส่ ID ของคุณตรงนี้";
   ```

---

## ส่วนที่ 2: อัปโหลดขึ้น GitHub (Static Deployment)
เนื่องจาก GitHub Pages ไม่รองรับ PHP เราจึงข้ามไปใช้ระบบ Formspree แทน ทำให้เว็บไซต์ของคุณเป็น **Static Site** ที่เสถียรและเร็วมากครับ

### 1. เตรียม Git (ในเครื่อง)
เปิด **Terminal** และพิมพ์คำสั่งตามนี้ (ทีละบรรทัด):
```bash
cd /Applications/MAMP/htdocs/my-portfolio
git init
git add .
git commit -m "Deploy: Professional Portfolio with Formspree"
```

### 2. สร้าง Repository บน GitHub
1. ไปที่ [GitHub.com](https://github.com) -> กด **New repository**
2. ตั้งชื่อว่า `my-portfolio` และเลือกเป็น **Public**
3. กด **Create repository**

### 3. เชื่อมต่อและอัปโหลด
ก๊อปปี้โค้ดจากหน้า GitHub ของคุณมาวาง (หน้าตาจะประมาณนี้):
```bash
git remote add origin https://github.com/ชื่อคุณ/my-portfolio.git
git branch -M main
git push -u origin main
```

---

## ส่วนที่ 3: เปิดใช้งานหน้าหน้าเว็บ (GitHub Pages)
1. ในหน้า Repository ของคุณบน GitHub -> ไปที่ **Settings**
2. เมนูด้านซ้ายเลือก **Pages**
3. ตรง **Branch** เลือก `main` และกด **Save**
4. รอ 1-2 นาที... คุณจะได้ลิงก์เว็บไซต์ เช่น `https://ชื่อคุณ.github.io/my-portfolio/`

---

**ยินดีด้วย!** เว็บไซต์ของคุณออนไลน์อย่างมืออาชีพแล้วครับ 🚀
